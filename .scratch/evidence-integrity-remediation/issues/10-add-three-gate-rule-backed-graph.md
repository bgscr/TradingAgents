# Add the three-gate rule-backed decision graph

Status: ready-for-agent

Blocked by: 09

Insert Evidence Preflight, strengthen Evidence Admission, isolate the final selector to a Validated Decision Context, implement versioned Strategy Rules and deterministic assertion validation, and harden the final Decision Gate. Quarantine shadow output as non-directional.

## Acceptance

- Preflight blockers invoke no analyst/model stages.
- Admission blockers invoke no research/trader/risk stages.
- The final selector receives no report, debate, trader, risk, or memory prose.
- Every directional assertion references an applicable registered rule whose predicate is recomputed.
- Final blocking writes no decision, signal, position fields, or memory.
- CLI and programmatic execution share graph routing and terminal validation.

## Comments

